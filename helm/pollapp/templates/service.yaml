apiVersion: v1
kind: Service
metadata:
  name: {{ include "pollapp.fullname" . }}
  labels:
    {{- include "pollapp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pollapp.selectorLabels" . | nindent 4 }}
