name: Build

on:
  workflow_dispatch:
  push:
    branches:
      - main

env:
  REGISTRY: quay.io
  IMAGE_NAME: wire/poll-app

jobs:
  deploy-staging:
    runs-on: ubuntu-22.04
    if: github.actor != 'github-actions[bot]'
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Login to Quay.io
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.QUAY_USERNAME }}
          password: ${{ secrets.QUAY_ROBOT_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          flavor: latest=false
          tags: |
            type=sha,format=short

      - name: Build and push
        uses: docker/build-push-action@v7
        with:
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}

#      - name: Update Helm values for staging with new image tag
#        id: update_helm_tag
#        run: |
#          TAG=$(echo "${{ steps.meta.outputs.tags }}" | grep -oE '[^:]+$')
#          echo "::set-output name=tag::$TAG"
#          sed -i 's/^\( *tag: \).*/\1"'$TAG'"/' helm/pollapp/values-staging.yaml
#        shell: bash
#
#      - name: Commit and push updated Helm values
#        run: |
#          git config --global user.name "github-actions[bot]"
#          git config --global user.email "github-actions[bot]@users.noreply.github.com"
#          git add helm/pollapp/values-staging.yaml
#          git commit -m "chore: Update staging Helm image tag to ${{ steps.update_helm_tag.outputs.tag }} [ci skip]"
#          git pull --rebase origin main
#          git push
#        env:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#        shell: bash

      # This build just pushes the image to quay.io, will add deploy steps when ArgoCD is set up
